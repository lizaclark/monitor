%include <qsub.h>
%include <head.h>
%manual
Run get_metstate.py.
Outputs are saved in the Met_Loc defined in config file.
Only run if a new state file needs to be created. Otherwise, use existing state.
%end

echo "Today is $(date)"

python %ECF_HOME%/processes/main/meteorological/get_metstate.py %WORKING_CFG_FILE%


%include <tail.h>
